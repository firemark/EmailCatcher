#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='EmailCatcher',
      version='0.1',
      description='Simple app to catch emails and view on site',
      author='Firemark',
      author_email='marpiechula@gmail.com',
      packages=find_packages()
     )